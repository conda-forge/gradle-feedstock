{% set version = "7.6.0" %}

package:
  name: gradle
  version: {{ version }}

source:
  url: https://github.com/gradle/gradle/archive/v{{ version }}.tar.gz
  sha256: dfd708de05faa4dfe2510d36facee473930bff6913b51a4fa4c973fdf84c1091

build:
  number: 0

requirements:
  build:
    - {{ compiler('c') }}
    - sysroot_linux-64 2.17  # [linux64]
  host:
    - openjdk >=11,<20

test:
  requires:
    - openjdk >=11,<20
    - m2-grep  # [win]
  commands:
    - gradle -v
    - gradle -v | grep -E "{{ version.replace(".", "\.") }}" || exit 1

about:
  home: https://gradle.org
  license: Apache-2.0
  license_file: LICENSE
  summary: Gradle - an open-source build-automation system
  description: |
    Gradle is an open-source build automation tool focused on flexibility and performance. Gradle build scripts are written using a Groovy or Kotlin DSL. Read about Gradle features to learn what is possible with Gradle.
  doc_url: https://docs.gradle.org/current/userguide/userguide.html
  dev_url: https://github.com/gradle/gradle

extra:
  recipe-maintainers:
    - klugem
